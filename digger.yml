projects:
  - name: dev
    dir: terraform/environments/dev
    workflow: default
    # Digger will run plan on PR, apply on comment "digger apply"
    include_patterns:
      - "terraform/environments/dev/**"
      - "terraform/modules/**"

auto_merge: false
collect_usage_data: false

locking:
  provider: aws
  aws:
    table_name: cknight17-digger-locks
    region: us-east-1

workflows:
  default:
    plan:
      steps:
        - init:
            extra_args: ["-backend-config=backend.hcl"]
        - plan
    apply:
      steps:
        - init:
            extra_args: ["-backend-config=backend.hcl"]
        - apply
